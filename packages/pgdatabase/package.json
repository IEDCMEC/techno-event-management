{
  "name": "pgdatabase",
  "main": "build/index.js",
  "types": "build/index.js",
  "dependencies": {
    "common": "workspace:*",
    "dotenv": "^16.0.3",
    "pg": "^8.11.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.3",
    "@types/pg": "^8.10.1",
    "rimraf": "^5.0.1",
    "tsconfig": "workspace:*",
    "typescript": "latest"
  },
  "scripts": {
    "lint": "eslint",
    "build": "npm run clean & npx tsc",
    "clean": "rimraf build",
    "dev": "npx tsc --watch"
  }
}
