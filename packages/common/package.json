{
  "name": "common",
  "main": "build/index.js",
  "types": "build/index.js",
  "dependencies": {
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/uuid": "^9.0.1",
    "rimraf": "^5.0.1",
    "tsconfig": "workspace:*",
    "typescript": "latest"
  },
  "scripts": {
    "lint": "eslint",
    "build": "npm run clean & npx tsc",
    "clean": "rimraf build",
    "dev": "npx tsc --watch"
  }
}
